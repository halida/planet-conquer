- content_for :head do
  = stylesheet_link_tag "room"
  = javascript_include_tag "room"

%h1 planet conquer

- game_server = "localhost:9999"
- room_id = 0
%canvas#room-canvas
:coffeescript
  game = new Game()
  shower = new GameShower(game, $('#room-canvas'))
  game.set_server("#{game_server}", #{room_id})

